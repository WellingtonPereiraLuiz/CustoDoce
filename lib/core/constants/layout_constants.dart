/// Breakpoint que separa o shell mobile (bottom nav, coluna única) do
/// shell desktop (sidebar fixa + topbar + grade de múltiplas colunas),
/// conforme `margin-desktop`/`max-width` do design system Stitch v2
/// (prd_custodoce_v2.md).
const double kDesktopBreakpoint = 1024;

/// Largura máxima do conteúdo em telas desktop (prd_custodoce_v2.md: max-width: 1200px).
const double kDesktopContentMaxWidth = 1200;
