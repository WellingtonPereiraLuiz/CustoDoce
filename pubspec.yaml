name: custo_doce
description: "CustoDoce - Calculadora de custos de receitas e precificação de produtos."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Persistence
  shared_preferences: ^2.3.0

  # State Management
  flutter_riverpod: ^2.5.1

  # Routing
  go_router: ^14.2.7

  # Local Database
  sqflite: ^2.3.3+1
  path: ^1.9.0

  # Monetization
  purchases_flutter: ^10.2.0

  # Backend / Cloud (future)
  supabase_flutter: ^2.5.7

  # UI Utilities
  intl: ^0.20.0
  flutter_slidable: ^3.1.0
  google_fonts: ^6.2.1
  gap: ^3.0.1
  fl_chart: ^0.70.0
  shimmer: ^3.0.0

  cupertino_icons: ^1.0.8
  purchases_ui_flutter: ^10.2.0
  sqflite_common_ffi_web: ^1.1.1
  url_launcher: ^6.3.2
  firebase_core: ^4.10.0
  firebase_auth: ^6.5.2
  google_sign_in: 6.2.1
  share_plus: ^13.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true

  assets:
    - assets/images/

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/CustoDoce.png"
  web:
    generate: true
    image_path: "assets/images/CustoDoce.png"
    background_color: "#1E1A18"
    theme_color: "#C5A059"
